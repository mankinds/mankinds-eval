# Scorer

The `Scorer` class is the main entry point for running evaluations.

::: mankinds_eval.scorer.base.Scorer
    options:
      members:
        - __init__
        - run
        - run_sync
        - from_config
        - to_dict
